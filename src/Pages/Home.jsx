import styled from "styled-components";
import Button from "../Components/Button";

const Container = styled.div`

`

export default function Home () {
    return (
        <Container>
            <Button name="por que me odeias?" />
        </Container>
    )
}